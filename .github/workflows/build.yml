name: Build Hermes Desktop Image

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Clone hermes-agent
        run: git clone --depth 1 https://github.com/NousResearch/hermes-agent.git

      - name: Get hermes-agent commit SHA
        id: hermes
        run: echo "sha=$(git -C hermes-agent rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Check if image already exists for this commit
        id: check
        run: |
          if docker manifest inspect ghcr.io/kasperhonore/hermes-desktop:${{ steps.hermes.outputs.sha }} > /dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Image for ${{ steps.hermes.outputs.sha }} already exists, skipping build"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "New commit ${{ steps.hermes.outputs.sha }}, building"
          fi

      - name: Log in to GHCR
        if: steps.check.outputs.exists == 'false'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        if: steps.check.outputs.exists == 'false'
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        if: steps.check.outputs.exists == 'false'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            HERMES_SHA=${{ steps.hermes.outputs.sha }}
          tags: |
            ghcr.io/kasperhonore/hermes-desktop:latest
            ghcr.io/kasperhonore/hermes-desktop:${{ steps.hermes.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
